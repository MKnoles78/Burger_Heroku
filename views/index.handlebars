{{!-- <img src="/assets/img/hamburger.png" id="burger"><br>


<h1 id="welcome">Welcome to Heroku Burger Bar</h1><br>

<h2>Place Order Here</h2>
<form class="create-form">
  <div class="form-group">
    <label for="create" id="burger-name">Burger Name:</label>
    <input type="text" id="createbgr" name="burger-name">
</div>
<button type="submit">Add burger</button>
</form>

<div class="container">
  <div class="row">
    <div class="col-sm">
      <ul>
        {{#each burgers}}
          {{#unless eat}}
          {{> burgers/burger-block eat=true}}
          {{/unless}}
        {{/each}}
    </ul>
    </div>
    <div class="col-sm">
    </div>
    <div class="col-sm">
      <ul>
        {{#each burgers}}
          {{#unless eat}}
          {{> burgers/burger-block eat=true}}
          {{/unless}}
        {{/each}}
    </ul>
    </div>
  </div>
</div> --}}

h1>Burgers!</h1>

<h2>Burgers that are not sleepy!</h2>

<ul>
  {{#each burgers}}
    {{#unless sleepy}}
      {{> burgers/burger-block sleep=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>burgers that are sleepy!</h2>

<ul>
  {{#each burgers}}
    {{#if sleepy}}
      {{> burgers/burger-block sleep=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="sleepy">Sleepy?</label><br>
    <input type="radio" name="sleepy" value="1" checked> Sleepy!<br>
    <input type="radio" name="sleepy" value="0"> Awake
  </div>

  <button type="submit">Add Burger</button>
</form>

